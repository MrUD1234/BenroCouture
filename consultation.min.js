document.addEventListener('DOMContentLoaded', () => { const isConsultation = document.body.classList.contains('consultation-page'); if (!isConsultation) return; const WEB3FORMS_ACCESS_KEY = 'd230d611-4ad3-4058-b345-bb11d1392b2e'; const WEB3FORMS_URL = 'https: let isSubmitting = false; const helpCards = document.querySelectorAll('.cs-help-card'); const consultTypeSelect = document.getElementById('consult-type'); const formSection = document.getElementById('cs-form'); if (helpCards.length && consultTypeSelect) { helpCards.forEach(card => { card.addEventListener('click', () => { const type = card.getAttribute('data-consult-type'); helpCards.forEach(c => c.classList.remove('selected')); card.classList.add('selected'); if (consultTypeSelect) { consultTypeSelect.value = type; consultTypeSelect.dispatchEvent(new Event('change', { bubbles: true })); } setTimeout(() => { formSection.scrollIntoView({ behavior: 'smooth', block: 'start' }); }, 300); }); }); } if (consultTypeSelect) { consultTypeSelect.addEventListener('change', () => { const selectedType = consultTypeSelect.value; helpCards.forEach(card => { card.classList.toggle( 'selected', card.getAttribute('data-consult-type') === selectedType ); }); }); } const datetimeRow = document.getElementById('cs-datetime-row'); const dateInput = document.getElementById('preferred-date'); const timeInput = document.getElementById('preferred-time'); function updateDateTimeRequired() { if (!consultTypeSelect || !datetimeRow) return; const type = consultTypeSelect.value; const isOptional = type === 'Fashion Academy' || type === 'General Enquiry'; datetimeRow.classList.toggle('cs-field-hidden', isOptional); if (dateInput) { if (isOptional) { dateInput.removeAttribute('required'); dateInput.removeAttribute('aria-required'); } else { dateInput.setAttribute('required', ''); dateInput.setAttribute('aria-required', 'true'); } } if (timeInput) { if (isOptional) { timeInput.removeAttribute('required'); timeInput.removeAttribute('aria-required'); } else { timeInput.setAttribute('required', ''); timeInput.setAttribute('aria-required', 'true'); } } } if (consultTypeSelect) { consultTypeSelect.addEventListener('change', updateDateTimeRequired); updateDateTimeRequired(); } function sanitizeValue(value) { return typeof value === 'string' ? value.trim().replace(/\s+/g, ' ') : ''; } function validateField(input) { const customSelect = input.closest('.cs-custom-select'); if (customSelect) { const errorEl = customSelect.parentElement.querySelector('.cs-form-error-message'); const value = sanitizeValue(input.value); const isEmpty = !value; if (isEmpty) { customSelect.classList.add('error'); if (errorEl) errorEl.textContent = 'This field is required.'; return false; } customSelect.classList.remove('error'); if (errorEl) errorEl.textContent = ''; return true; } const errorEl = input.parentElement.querySelector('.cs-form-error-message'); const value = sanitizeValue(input.value); const isEmpty = input.tagName === 'SELECT' ? !value : !value; if (isEmpty) { input.classList.add('error'); if (errorEl) errorEl.textContent = 'This field is required.'; return false; } if (input.type === 'email') { const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/; if (!emailPattern.test(value)) { input.classList.add('error'); if (errorEl) errorEl.textContent = 'Please enter a valid email address.'; return false; } } if (input.type === 'tel') { const phonePattern = /^(?:\+234|234|0)[0-9]{10}$/; if (!phonePattern.test(value.replace(/[\s\-\(\)]/g, ''))) { input.classList.add('error'); if (errorEl) errorEl.textContent = 'Please enter a valid Nigerian phone number.'; return false; } } input.classList.remove('error'); if (errorEl) errorEl.textContent = ''; return true; } const initCustomSelects = () => { const wrappers = document.querySelectorAll('.cs-custom-select'); const closeAll = (exclude) => { wrappers.forEach(w => { if (w !== exclude) w.classList.remove('open'); const t = w.querySelector('.cs-custom-select-trigger'); if (t) t.setAttribute('aria-expanded', 'false'); }); }; wrappers.forEach(wrapper => { const trigger = wrapper.querySelector('.cs-custom-select-trigger'); const valueEl = wrapper.querySelector('.cs-custom-select-value'); const menu = wrapper.querySelector('.cs-custom-select-menu'); const options = wrapper.querySelectorAll('.cs-custom-select-option'); const native = wrapper.querySelector('.cs-custom-select-native'); if (!trigger || !native) return; const defaultText = native.options.length > 0 ? native.options[0].textContent : ''; trigger.addEventListener('click', () => { const wasOpen = wrapper.classList.contains('open'); closeAll(wrapper); if (!wasOpen) { wrapper.classList.add('open'); trigger.setAttribute('aria-expanded', 'true'); } }); if (menu) { menu.addEventListener('click', (e) => { if (e.target === menu) { wrapper.classList.remove('open'); trigger.setAttribute('aria-expanded', 'false'); trigger.focus(); } }); } options.forEach(opt => { opt.addEventListener('click', () => { native.value = opt.getAttribute('data-value'); native.dispatchEvent(new Event('change', { bubbles: true })); wrapper.classList.remove('open'); trigger.setAttribute('aria-expanded', 'false'); trigger.focus(); }); }); native.addEventListener('change', () => { const val = native.value; const matched = [...options].find(o => o.getAttribute('data-value') === val); if (matched) { valueEl.textContent = matched.textContent; valueEl.classList.add('filled'); options.forEach(o => o.classList.remove('selected')); matched.classList.add('selected'); } else { valueEl.textContent = defaultText; valueEl.classList.remove('filled'); options.forEach(o => o.classList.remove('selected')); } validateField(native); }); trigger.addEventListener('keydown', (e) => { if (e.key === 'Escape') { wrapper.classList.remove('open'); trigger.setAttribute('aria-expanded', 'false'); } }); trigger.addEventListener('blur', (e) => { if (!wrapper.contains(e.relatedTarget)) { validateField(native); } }); }); document.addEventListener('click', (e) => { if (!e.target.closest('.cs-custom-select')) { closeAll(); } }); }; initCustomSelects(); const form = document.getElementById('consultation-form'); const submitBtn = document.getElementById('cs-submit-btn'); const btnText = submitBtn ? submitBtn.querySelector('.cs-btn-text') : null; const formError = document.getElementById('cs-form-error'); function showLoading() { if (!submitBtn) return; submitBtn.classList.add('loading'); if (btnText) btnText.textContent = 'Sending...'; submitBtn.disabled = true; } function hideLoading() { if (!submitBtn) return; submitBtn.classList.remove('loading'); if (btnText) btnText.textContent = 'Book Consultation'; submitBtn.disabled = false; } function showSuccess() { if (!form) return; form.classList.add('fade-out'); setTimeout(() => { const successEl = document.createElement('div'); successEl.className = 'cs-form-success visible'; successEl.setAttribute('role', 'status'); successEl.innerHTML = '<div class="cs-form-success-icon">&#10003;</div>' + '<h3 class="cs-form-success-heading">Consultation Request Received</h3>' + '<p class="cs-form-success-text">Thank you for choosing BenroCouture. We\'ve received your consultation request successfully. Our team will contact you within 24 hours using your preferred contact method.</p>' + '<p class="cs-form-success-text" style="margin-top:16px;">While you wait, feel free to continue exploring our latest collections.</p>' + '<a href="lookbook.html" class="btn btn-outline">View Lookbook</a>'; form.innerHTML = ''; form.appendChild(successEl); form.classList.remove('fade-out'); const liveRegion = document.createElement('div'); liveRegion.setAttribute('aria-live', 'polite'); liveRegion.className = 'sr-only'; liveRegion.textContent = 'Consultation request submitted successfully.'; document.body.appendChild(liveRegion); setTimeout(function () { liveRegion.remove(); }, 3000); }, 500); } function showError() { if (!formError) return; formError.removeAttribute('hidden'); formError.classList.add('visible'); formError.scrollIntoView({ behavior: 'smooth', block: 'center' }); formError.focus(); } function hideError() { if (!formError) return; formError.setAttribute('hidden', ''); formError.classList.remove('visible'); } function buildFormData() { var data = new FormData(); var fields = form.querySelectorAll('[name]'); fields.forEach(function (field) { var name = field.getAttribute('name'); if (name === 'botcheck' || name === 'access_key' || name === 'subject' || name === 'from_name') return; var value = sanitizeValue(field.value); if (value) { data.append(name, value); } }); data.append('access_key', WEB3FORMS_ACCESS_KEY); data.append('subject', 'New Consultation Request - BenroCouture'); data.append('from_name', 'BenroCouture Website'); var now = new Date(); var timezone = Intl.DateTimeFormat ? Intl.DateTimeFormat().resolvedOptions().timeZone : 'Not available'; var dateOptions = { year: 'numeric', month: 'long', day: 'numeric' }; var timeOptions = { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: true }; data.append('Date Submitted', now.toLocaleDateString('en-US', dateOptions)); data.append('Time Submitted', now.toLocaleTimeString('en-US', timeOptions)); data.append('Timezone', timezone); return data; } if (form) { var requiredInputs = form.querySelectorAll('[required]'); requiredInputs.forEach(function (input) { input.addEventListener('blur', function () { validateField(input); }); input.addEventListener('input', function () { if (input.classList.contains('error')) { validateField(input); } }); }); form.addEventListener('submit', function (e) { e.preventDefault(); hideError(); if (isSubmitting) return; isSubmitting = true; var activeRequired = form.querySelectorAll('[required]'); var isValid = true; activeRequired.forEach(function (input) { if (!validateField(input)) { isValid = false; } }); if (!isValid) { isSubmitting = false; var firstError = form.querySelector('.cs-form-input.error') || form.querySelector('.cs-custom-select.error'); if (firstError) { firstError.scrollIntoView({ behavior: 'smooth', block: 'center' }); if (firstError.classList.contains('cs-custom-select')) { var t = firstError.querySelector('.cs-custom-select-trigger'); if (t) t.focus(); } else { firstError.focus(); } } return; } showLoading(); fetch(WEB3FORMS_URL, { method: 'POST', body: buildFormData() }) .then(function (response) { return response.json(); }) .then(function (result) { if (result.success) { showSuccess(); } else { hideLoading(); showError(); isSubmitting = false; } }) .catch(function () { hideLoading(); showError(); isSubmitting = false; }); }); } const revealElements = [...document.querySelectorAll('[data-reveal]')].filter( el => !el.closest('.site-footer') ); const revealObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('revealed'); revealObserver.unobserve(entry.target); } }); }, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' }); revealElements.forEach(el => revealObserver.observe(el));
});